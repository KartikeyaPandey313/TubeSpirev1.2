# ==============================================================================
# .gitignore for TubeSpire
#
# This file specifies intentionally untracked files and folders that Git
# should ignore. This is crucial for keeping the repository clean and secure.
# ==============================================================================

# --- Security ---
# Never commit files containing secret keys, passwords, or API tokens.
# The .env file is used for local development secrets.
.env
.env.*

# --- Python Virtual Environments ---
# Exclude virtual environment folders, as they are specific to a user's machine
# and can be rebuilt from requirements.txt.
venv/
.venv/
env/
*.env

# --- Python Cache & Compiled Files ---
# Ignore Python's cache directories and compiled files, as they are
# generated automatically at runtime.
__pycache__/
*.py[cod]
*.so

# --- Application Data ---
# Exclude the temporary folder where downloaded media is stored before delivery.
# This folder should not be part of the codebase.
downloads/

# --- IDE & Editor Configuration ---
# Exclude configuration files and directories generated by common code editors
# to avoid cluttering the repository with user-specific settings.
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# --- Operating System Files ---
# Ignore metadata files generated by macOS and Windows.
.DS_Store
Thumbs.db

# --- Log Files ---
# Exclude log files, as they are runtime-specific and not part of the source.
*.log
logs/

# --- Documentation & Project Files ---
# Note: It is standard practice to commit these files to your repository.
guide/
extras/
